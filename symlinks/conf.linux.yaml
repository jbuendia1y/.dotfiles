- clean: ['~']

- defaults:
    link:
      create: true
      force: true

- link:
    ~/.dotly: os/linux/.dotly
    ~/.config/starship.toml: os/common/starship/starship.toml
    ~/.config/nvim: editors/nvim
